import React from "react";
import { Link } from "react-router-dom";

const Contacto = () => {
    return (
        <div>
        <h1>Contacto</h1>
        <Link to="/">Inicio</Link>
        </div>
    );
    };

export default Contacto;
